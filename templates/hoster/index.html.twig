{% extends 'base.html.twig' %}

{% block title %}Hoster index{% endblock %}

{% block body %}
    <h1>Hoster index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for hoster in hosters %}
            <tr>
                <td>{{ hoster.id }}</td>
                <td>{{ hoster.name }}</td>
                <td>
                    <a href="{{ path('app_hoster_show', {'id': hoster.id}) }}">show</a>
                    <a href="{{ path('app_hoster_edit', {'id': hoster.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_hoster_new') }}">Create new</a>
{% endblock %}
