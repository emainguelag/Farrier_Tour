<form method="post" action="{{ path('app_hoster_delete', {'id': hoster.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hoster.id) }}">
    <button class="btn">Delete</button>
</form>
