{% extends 'base.html.twig' %}

{% block title %}Hoster{% endblock %}

{% block body %}
    <h1>Hoster</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hoster.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hoster.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hoster_index') }}">back to list</a>

    <a href="{{ path('app_hoster_edit', {'id': hoster.id}) }}">edit</a>

    {{ include('hoster/_delete_form.html.twig') }}
{% endblock %}
