{% extends 'base.html.twig' %}

{% block title %}Farrier{% endblock %}

{% block body %}
    <h1>Farrier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farrier.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ farrier.name }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ farrier.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_farrier_index') }}">back to list</a>

    <a href="{{ path('app_farrier_edit', {'id': farrier.id}) }}">edit</a>

    {{ include('farrier/_delete_form.html.twig') }}
{% endblock %}
