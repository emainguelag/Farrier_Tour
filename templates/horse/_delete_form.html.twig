<form method="post" action="{{ path('app_horse_delete', {'id': horse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horse.id) }}">
    <button class="btn">Delete</button>
</form>
